{
  "name": "canvas-calculator-playwright-ts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "playwright install --with-deps",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "dependencies": {
    "canvas": "^3.2.0",
    "dotenv": "^17.2.1",
    "tesseract.js": "^6.0.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
